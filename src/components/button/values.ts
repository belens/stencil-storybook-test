export namespace ButtonValues {
  export enum Size {
    SMALL = 'small',
    MEDIUM = 'medium',
    LARGE = 'large',
  }

  export enum Variant {
    PRIMARY = 'primary',
    SECONDARY = 'secondary',
    FLAT = 'flat',
    LINE = 'line',
    DESTRUCTIVE = 'destructive',
  }

  export enum Type {
    BUTTON = 'button',
    SUBMIT = 'submit',
    RESET = 'reset',
  }
}
